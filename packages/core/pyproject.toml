[project]
name = "deep-research-core"
version = "2.0.0"
description = "Core libraries for deep-research project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/deep_research_core"]
